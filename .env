# Development environment variables
# Used when running `npm run dev` or `npm run preview`
# For production builds, the app uses NBA CDN directly (no proxy needed)
# Uncomment the line below only if you need a custom proxy
# VITE_NBA_API_BASE=/api/nba
VITE_NBA_TIMEOUT_MS=8000
