{
  "name": "even-nba-pulse",
  "version": "0.2.8",
  "description": "Live NBA scores, game state, and play-by-play timeline for Even G2 smart glasses.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test",
    "qr": "evenhub qr --http --port 5173",
    "pack": "npm run build && evenhub pack app.json dist -o even-nba-pulse.ehpk --check"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.9"
  },
  "devDependencies": {
    "@evenrealities/evenhub-cli": "^0.1.10",
    "vite": "^7.3.1"
  }
}
